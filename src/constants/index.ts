const ORIGIN = '*';
const PORT = 3000;
const NODE_ENV = process.env.NODE_ENV || 'development';
const ACCESS_TOKEN_PRIVATE_KEY = 'access';
const REFRESH_TOKEN_PRIVATE_KEY = 'refresh';

export { ORIGIN, PORT, NODE_ENV, ACCESS_TOKEN_PRIVATE_KEY, REFRESH_TOKEN_PRIVATE_KEY };
